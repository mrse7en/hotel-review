'use strict';

/**
 * @ngdoc function
 * @name HotelReview.controller:MainController
 * @description
 * # MainController
 * This controller handles the side menu
 */
angular.module('HotelReview')
    .controller('MainController', function($scope, $timeout, $ionicLoading) {
		  // Setup the loader
		  /*$ionicLoading.show({
			content: 'Loading',
			animation: 'fade-in',
			showBackdrop: true,
			maxWidth: 200,
			showDelay: 0,
			template: "<img src='images/logo.png' width='100px'>"
		  });
		 // $ionicLoading.html("test");
		  $timeout(function () {
			$ionicLoading.hide();
		  }, 3000);*/
		  

    });
